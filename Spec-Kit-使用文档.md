# Spec Kit 使用文档（大白话版）

## 1. 先一句话说清楚：Spec Kit 是什么？

**Spec Kit 是一套“先把需求写清楚，再让 AI 按步骤做”的开发工具包。**

你可以把它想成：

- 不是一上来就让 AI 写代码
- 而是先让 AI 帮你把事情说明白
- 再把说明变成计划
- 再把计划拆成任务
- 最后再开始真正动代码

它的目标不是“让 AI 随便发挥”，而是：

> **让 AI 干活更像一个有流程、有家规、有施工图的团队成员。**

顺便提醒一个容易搞混的点：

- 项目名字叫 **Spec Kit**
- 终端命令叫 **`specify`**
- 聊天里常用的命令是 **`/speckit.xxx`**

---

## 2. 它主要解决什么问题？

如果你平时已经在用 AI 写代码，可能会遇到这些问题：

### 问题 1：AI 写得很快，但经常理解错
比如你说：

> “帮我做一个照片管理功能。”

AI 可能自己脑补很多事：

- 照片要不要上传云端？
- 需不需要登录？
- 相册能不能嵌套？
- 需不需要拖拽排序？

结果它写得很热闹，但不一定是你真正要的。

### 问题 2：聊着聊着，需求越来越乱
一开始说了 A，后来补了 B，中间又加了 C。  
过几轮对话以后：

- 你自己都快忘了最初要求
- AI 更容易忘
- 最终代码和原始想法对不上

### 问题 3：项目做完后，没人说得清为什么这么做
只剩代码，没有清楚的“变更说明”。

---

Spec Kit 的思路就是：

> **先把“要做什么”变成一套可执行说明，再让 AI 去做。**

它不是只关心“代码能不能生成出来”，而是更关心：

- 需求是不是说清楚了
- 方案是不是合理
- 任务是不是拆清楚了
- 实现是不是和前面说的一致

---

## 3. 它的核心思路，用一个装修例子来理解

如果把开发软件比作装修房子：

### 普通 AI 写代码方式
像是你对装修队说：

> “给我装个好看的书房。”

然后对方直接开工。

结果可能：

- 颜色不是你想要的
- 插座位置不对
- 书柜尺寸不合适
- 做完后返工很多

### Spec Kit 的方式
它更像是先做下面几件事：

1. 先确认你到底想要什么房间
2. 把需求写出来
3. 再出施工方案
4. 再拆成施工步骤
5. 最后才开工

所以它不只是“叫 AI 干活”，而是先把活儿描述成一份比较完整的“施工说明书”。

---

## 4. Spec Kit 怎么安装？

官方文档给出的前提条件大概是这些：

- 电脑能用 **Python 3.11+**
- 装了 **Git**
- 有一个它支持的 AI 编码工具
  - 比如 Claude Code、GitHub Copilot、Gemini 等
- 推荐用 **uv** 来安装

### 官方推荐安装方式

```bash
uv tool install specify-cli --from git+https://github.com/github/spec-kit.git@vX.Y.Z
```

这里的 `vX.Y.Z` 要替换成官方发布版本号。

### 验证安装

```bash
specify version
```

### 一个很重要的注意点
官方文档特别提醒：

> **以 GitHub 仓库里的版本为准。**

也就是说，正常情况下你应该从 GitHub 这边安装，不要随便装一个同名包就当成官方版。

---

## 5. 初始化后会发生什么？

初始化一个项目，通常这样写：

```bash
specify init my-project --integration claude
```

或者在当前目录初始化：

```bash
specify init . --integration claude
```

这里的 `--integration claude`，你可以理解成：

**告诉 Spec Kit，你打算配合哪个 AI 工具一起用。**

比如你用 Claude Code、Copilot、Gemini，不同工具的接法会稍微不一样。

初始化以后，它会大致帮你准备几类东西：

- 项目里的 Spec Kit 目录和模板
- 给 AI 工具用的命令或技能文件
- 后面做需求、出计划、拆任务的基础结构

你可以把这一步理解成：

> **把施工现场搭好，把工具箱和流程卡片先发下去。**

---

## 6. 它最常见的使用流程

Spec Kit 推荐的核心流程，最简单可以理解成这几步：

```text
/speckit.constitution
→ /speckit.specify
→ /speckit.plan
→ /speckit.tasks
→ /speckit.implement
```

如果你更严谨一点，还会加上：

```text
/speckit.clarify
/speckit.checklist
/speckit.analyze
```

下面我用大白话逐个解释。

---

## 7. 每个命令到底是在干嘛？

## 7.1 `/speckit.constitution`
这个词看起来有点大，其实你可以把它理解成：

**给项目立“家规”。**

比如你可以先告诉 AI：

- 代码质量要高
- 测试要认真做
- 页面体验要统一
- 性能不能太差
- 不要乱加没必要的复杂东西

比如：

```text
/speckit.constitution 为这个项目制定规则：重视代码质量、测试、统一体验和性能
```

这一步的意思不是做功能，  
而是先告诉 AI：

> “以后你干活，按这些原则来。”

这很像公司里的开发规范，或者团队共识。

---

## 7.2 `/speckit.specify`
这是最关键的一步之一。

它的作用是：

**把“我要做什么”写成一份清楚的需求说明。**

注意这里强调的是：

- 说清楚 **做什么**
- 说清楚 **为什么做**
- 暂时别急着说太多“技术怎么实现”

比如：

```text
/speckit.specify 做一个照片整理应用。用户可以创建相册、按日期分组、拖拽调整相册顺序、在相册里用宫格方式预览照片。
```

这一阶段最像产品需求整理。

你可以把它理解成：

> “先把目标和用户场景讲明白。”

---

## 7.3 `/speckit.clarify`
这个命令特别实用。

它的作用是：

**找出需求里说得不够清楚的地方，然后补问、补全。**

比如你只是说“做个登录功能”，那里面其实有很多没说清楚的地方：

- 用手机号还是邮箱？
- 要不要验证码？
- 忘记密码怎么办？
- 需不需要第三方登录？

`clarify` 就像一个认真负责的人，先不瞎猜，而是追问你：

> “这些地方你还没讲明白，要不要补充？”

这一步能明显减少 AI 自己脑补导致的跑偏。

---

## 7.4 `/speckit.checklist`
这个命令可以理解成：

**给需求做一次查漏补缺。**

大白话讲，就是看你的需求是不是：

- 有空白
- 有歧义
- 有互相打架的地方
- 有说了但不够具体的地方

你可以把它想成：

> “给需求做一遍体检。”

在真正进入方案和开发前，先查漏，通常能少掉不少返工。

---

## 7.5 `/speckit.plan`
这一步才开始谈“怎么做”。

前面的 `specify` 是：

- 要做什么
- 为什么做

而 `plan` 是：

- 准备怎么实现
- 用什么技术路线
- 关键结构怎么安排

比如：

```text
/speckit.plan 前端尽量简单，数据存在本地数据库里，界面支持拖拽，先不用复杂框架。
```

你可以把它理解成：

> “需求书写完了，现在开始出施工方案。”

---

## 7.6 `/speckit.tasks`
这一步是把大方案拆成可执行的小任务。

例如一开始你只是说：

- 做照片相册

但到任务阶段，可能就会拆成：

- 建相册列表页面
- 支持创建相册
- 支持拖拽排序
- 支持照片宫格预览
- 支持按日期分组
- 补测试

这一步很像项目经理把事情切成一张张待办卡片。

所以它的核心价值是：

> **把“模糊的大目标”变成“可执行的小步骤”。**

---

## 7.7 `/speckit.analyze`
这个命令可以理解成：

**在开工前，帮你把前面的文档互相对一遍。**

它会看：

- 需求和计划是不是对得上
- 计划和任务是不是对得上
- 有没有漏任务
- 有没有前后矛盾

你可以把它理解成：

> “需求书、施工图、任务单，三份文件拿出来互相核对。”

这一步对复杂项目挺有价值。

---

## 7.8 `/speckit.implement`
这一步就是：

**真正开始干活，让 AI 按任务去实现。**

前面那些都像准备动作，  
这里才是正式进场施工。

如果前面的需求、计划、任务已经整理得不错，这一步通常会更顺。

---

## 8. 一个完整例子：做一个“照片相册应用”

如果你还是觉得抽象，可以看这个例子。

### 第一步：先立规矩

```text
/speckit.constitution 这个项目重视代码质量、测试、页面一致性和性能
```

意思是先告诉 AI：以后你干活别太随意。

### 第二步：定义需求

```text
/speckit.specify 做一个照片整理应用。用户可以创建相册，按日期分组，相册之间能拖拽排序，照片在相册里用宫格显示。
```

### 第三步：补清楚模糊点

```text
/speckit.clarify 请重点确认相册排序、照片预览、是否需要登录这几个点
```

### 第四步：检查需求有没有漏洞

```text
/speckit.checklist
```

### 第五步：写实现方案

```text
/speckit.plan 尽量少用复杂依赖，图片不上传云端，信息存在本地数据库里
```

### 第六步：拆任务

```text
/speckit.tasks
```

### 第七步：开工前再核对一次

```text
/speckit.analyze
```

### 第八步：开始实现

```text
/speckit.implement
```

这个流程背后的核心思想就是：

> **先把事情说清楚，再把路铺清楚，最后才让 AI 开始狂奔。**

---

## 9. Spec Kit 的一个明显特点：它很强调“流程”

这一点既是优点，也可能是缺点。

它不像那种“你随便一句话，AI 立刻开写”的方式。  
它更像：

- 先需求
- 再补问
- 再检查
- 再方案
- 再任务
- 再实现

如果你喜欢这种做事方式，会觉得很稳。

如果你只想快速改个小功能，可能会觉得：

**“步骤是不是有点太多了？”**

这点我后面在“不足”里会重点说。

---

## 10. Spec Kit 的主要特点

## 10.1 流程完整，比较稳

它不是只给你一个“写代码”的命令，  
而是把开发拆成一整条链路。

这意味着它比较适合：

- 需求容易跑偏的项目
- 比较重要的功能
- 团队协作场景
- 想把过程留档的项目

简单说：

> **它更重视“做对”，不只是“做快”。**

---

## 10.2 很适合和 AI 搭配使用

Spec Kit 本来就是为 AI 开发流程准备的。  
它不是单纯的文档模板，而是把 AI 真正放进流程里：

- AI 帮你整理需求
- AI 帮你出方案
- AI 帮你拆任务
- AI 帮你落地实现

所以它不是“给人看的静态文档工具”，而是：

**让文档、流程和 AI 协作连在一起。**

---

## 10.3 支持很多 AI 工具

官方说它支持很多 AI 编码工具，不是只绑定一种。

这意味着如果你们团队工具不统一，比如有人用：

- Claude Code
- GitHub Copilot
- Gemini
- Codex

那 Spec Kit 的通用性会比较有吸引力。

---

## 10.4 会把每个功能拆成独立编号，比较清楚

官方文档提到，它会根据功能生成编号和目录，比如：

- `001-photo-albums`
- `002-login-flow`
- `003-chat-system`

你可以理解成：

**每个功能都是一个独立小档案。**

好处是：

- 不容易混
- 回头找方便
- 分支和文档能对应起来
- 团队协作时更清楚谁在做什么

---

## 10.5 有“质量关卡”意识

`clarify`、`checklist`、`analyze` 这几个命令，本质上都在做一件事：

**尽量在写代码前，先把问题找出来。**

这非常像施工前先审图，而不是房子砌到一半才发现门开错了方向。

这个特点很适合：

- 对质量要求高的项目
- 返工成本高的项目
- 需求容易模糊的大项目

---

## 10.6 可定制能力强

Spec Kit 有两套可定制玩法：

- **Extensions**：你可以理解成“插件”
- **Presets**：你可以理解成“模板包 / 套餐”

大白话讲：

### Extension 像什么？
像给工具装新功能。  
比如增加和 Jira、代码审查、额外检查流程有关的能力。

### Preset 像什么？
像给流程换一套模板和说话方式。  
比如：

- 某公司要更严格的审批格式
- 某团队要固定任务写法
- 某行业要特殊的合规说明

如果你的团队很在意规范统一，这个能力会很实用。

---

## 10.7 它不只是适合新项目，也适合老项目

官方把场景分成：

- 从 0 到 1 新项目
- 创意探索
- 老项目迭代改造

也就是说，它不是只能用来“从零做一个东西”。  
如果你是维护老系统、分阶段加功能，它也能用。

不过它更适合“正式一点的变更”，不是特别碎的小改动。

---

## 11. Spec Kit 的不足和注意点

这部分我尽量说直白一点。

## 11.1 对小改动来说，可能太重了

这是我觉得最明显的点。

如果你只是：

- 改一个按钮文案
- 修一个小样式
- 补一个小判断
- 调一个接口地址

那你还要走：

- constitution
- specify
- clarify
- checklist
- plan
- tasks
- analyze
- implement

你很可能会觉得：

> “我只是想改个小东西，怎么像要写毕业论文一样？”

所以它不太适合所有事都上。  
更适合：

- 重要功能
- 中大型改动
- 容易返工的需求

---

## 11.2 学习成本比“直接让 AI 写”高

它里面有不少自己的概念：

- constitution
- specify
- clarify
- analyze
- preset
- extension

这些并不难，但第一次接触时，肯定比“直接跟 AI 说一句话”更复杂。

也就是说：

**它上手不是最轻的那类工具。**

---

## 11.3 安装和接入门槛稍微高一点

它不是那种点一下就能马上用的网页小工具。  
你通常要准备：

- Python 环境
- Git
- 安装命令
- 选 AI 工具集成方式

如果你本身就熟悉命令行，这不算大问题。  
但如果你想“零配置马上用”，它就没那么轻松。

---

## 11.4 很依赖你是否愿意认真维护流程

Spec Kit 的价值，建立在一个前提上：

> **你真的愿意先写清楚，再让 AI 干活。**

如果团队现实情况是：

- 大家嫌麻烦
- 需求写一半就不更新
- 任务拆完也不回头看
- 代码改完不对账

那 Spec Kit 再强，也会慢慢变成：

- 文档一套
- 实际做法一套
- 最后谁也不信这些文档

所以它不是装上就自动提升质量，  
它需要团队有一点流程意识。

---

## 11.5 它有比较强的“做事口味”，不一定适合所有团队

从文档里能看出来，Spec Kit 有一套比较明确的偏好，比如：

- 很重视先定规则
- 很重视先补需求
- 很重视测试和一致性
- 很反对过度复杂

这些方向很多时候是好事。  
但反过来说，如果你的团队习惯完全不一样，可能会觉得它“管得有点多”。

简单讲：

**它不是中性到毫无个性的工具，它是有方法论倾向的。**

---

## 11.6 对 AI 的理解能力要求不低

官方文档里自己也提到，它比较依赖高级 AI 模型的能力。  
因为它不是只让 AI 写几行代码，而是要 AI：

- 理解需求
- 补充模糊点
- 做计划
- 拆任务
- 做一致性检查
- 最后再实现

所以如果 AI 本身理解力一般，或者你给的描述非常含糊，效果就会打折。

换句话说：

> **它不是“随便什么 AI 都能发挥得很好”的工具。**

---

## 11.7 对已有工作方式可能有冲击

Spec Kit 会引入它自己的：

- 命令体系
- 目录结构
- 功能编号方式
- 分支工作方式

如果你原来的团队已经有一套很成熟的做法，接入时就要考虑：

- 会不会和现有流程冲突
- 团队愿不愿意适应
- 大家能不能接受多一层流程

所以它不是“无感接入”的那种工具，  
更像是：

**你要么接受它的一部分方法，要么就只能浅用。**

---

## 12. 适合什么人用？

我觉得它比较适合下面这些人：

### 很适合
- 经常用 AI 写代码的人
- 做中大型项目的人
- 希望少返工的人
- 团队协作比较多的人
- 想把需求、计划、任务都沉淀下来的人
- 对“过程清晰”比“马上开写”更重视的人

### 不一定适合
- 只做很小的个人 demo
- 经常只改一点点小东西
- 很讨厌流程和文档的人
- 团队执行力很弱、文档大概率没人维护的场景
- 只想快速让 AI 出个原型的人

---

## 13. 怎么判断你要不要上 Spec Kit？

你可以问自己这几个问题：

### 问题 1：我是不是经常被 AI 写偏？
如果是，Spec Kit 很可能有帮助。

### 问题 2：我的需求是不是经常改？
如果是，它这种“先整理需求再改”的方式会更稳。

### 问题 3：我的项目是不是比较重要，返工成本高？
如果是，前面多花点时间是值得的。

### 问题 4：我是不是只是想快速搞个小玩具？
如果是，Spec Kit 可能显得偏重。

---

## 14. 我对 Spec Kit 的一句评价

如果要用最直白的话来说：

> **Spec Kit 是给 AI 开发加上“家规、说明书、检查单、任务单”的工具。**

它不是让 AI 更自由，  
而是让 AI **更可控、更有章法**。

这会让它在复杂项目里更值钱，  
但在小项目里也更容易显得“有点重”。

---

## 15. 新手怎么用最合适？

如果你第一次用，我建议不要一开始就想把全部功能吃透。  
可以先从这条主线开始：

```text
/speckit.constitution
/speckit.specify
/speckit.plan
/speckit.tasks
/speckit.implement
```

等你觉得顺手了，再把这几个“质量关卡”加上：

```text
/speckit.clarify
/speckit.checklist
/speckit.analyze
```

如果你怕流程太重，也可以按官方建议，先用简化流程做实验；  
如果是正式功能，再把补问、检查、分析全加上。

---

## 16. 常用命令速查

### 安装与检查

```bash
uv tool install specify-cli --from git+https://github.com/github/spec-kit.git@vX.Y.Z
specify version
specify check
```

### 初始化项目

```bash
specify init my-project --integration claude
specify init . --integration claude
specify init --here --integration claude
```

### 核心聊天命令

```text
/speckit.constitution
/speckit.specify
/speckit.plan
/speckit.tasks
/speckit.implement
```

### 常用补充命令

```text
/speckit.clarify
/speckit.checklist
/speckit.analyze
/speckit.taskstoissues
```

### 自我检查与升级

```bash
specify self check
specify self upgrade --dry-run
specify self upgrade
```

---

## 17. 结论

如果你现在已经在用 AI 写代码，而且你越来越在意这些事：

- 需求能不能讲清楚
- AI 会不会跑偏
- 团队能不能少返工
- 项目能不能把过程沉淀下来

那 Spec Kit 是一个值得认真看的工具。

但它最适合的姿势不是：

> **所有事都强行走全流程。**

而是：

> **把它用在那些真正重要、复杂、容易返工、需要留痕的功能上。**

这么用，它的价值会比较明显。

---

## 18. 参考资料

- [Spec Kit GitHub 仓库](https://github.com/github/spec-kit)
- [README](https://github.com/github/spec-kit/blob/main/README.md)
- [Quick Start Guide](https://github.com/github/spec-kit/blob/main/docs/quickstart.md)
- [Installation Guide](https://github.com/github/spec-kit/blob/main/docs/installation.md)
- [Specification-Driven Development](https://github.com/github/spec-kit/blob/main/spec-driven.md)
- [Core Commands](https://github.com/github/spec-kit/blob/main/docs/reference/core.md)
- [Integrations Reference](https://github.com/github/spec-kit/blob/main/docs/reference/integrations.md)
- [Extensions Reference](https://github.com/github/spec-kit/blob/main/docs/reference/extensions.md)
- [Presets Reference](https://github.com/github/spec-kit/blob/main/docs/reference/presets.md)
